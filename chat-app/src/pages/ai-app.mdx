# AI App 🦜

<Callout title="Info" variant="default">
Before using this app, please follow these steps:

1 Install dependencies:

```bash
pip install langchain langchain-openai
```

2 Set the `OPENAI_API_KEY` in `.env` file.

```bash
OPENAI_API_KEY=sk-...
```
</Callout>

<Chat postData="langchain_chat" height={400} />
